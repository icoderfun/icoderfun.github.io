---
layout: post
title: "Go 算法练习第一篇：从零实现冒泡排序与性能分析"
date: 2025-10-28 21:00:00 +0800
categories: [编码, 技术]
---
## 前言
职业生涯在10年左右开始长时间从事管理工作，现在又大量使用 AI 进行辅助编码，我发现自己的很多基本功都废了，一些基础的算法和数据结构已经都忘了，所以启动了本系列 Go 算法练习，希望能够在「业余时间」，进行编码练习，逐步找回「编码的乐趣」（详见《[[找到编程的乐趣]]》，并且在 AI 的辅助下指定了[[编程成长计划]],今天是计划实施第一天，简单的冒泡算法开始，进行系统性训练，提升自我。

并且我希望能够将自己的总结和代码都发表出来，无论是否有人看，我都会作为激励自己的手段，逐渐找回乐趣，做一些让自己开心的事情。


### 题目解析

什么是冒泡排序？

这个名字形象的说明了这个排序规则，在进行一个（例如从小到大）排序的时候，就像冒泡一样，循环将每一个元素和其他剩余元素做对比，如果顺序不一致（例如从小到大）则交换元素，完成一遍之后再进行一遍，直到某一遍循环没有发生交换，则中止，最终实现序列的排序。


举个例子：

输入序列：\[22,12,33,3\]

目标序列：\[3,12,22,33\]

方法：

- 每一遍循环，都将第一个数和后边的数做对比，如果比后边的大则交换
- 由于每完成一次遍历，都会将最大的数放到最后，所以每次循环，我们都可以少遍历一个（即 n-1-i) 。
- 最终当完成一次遍历，发现没有发生交换，则中止。

示意图：

/db628c2f-edfe-4564-8358-0d1e506dcc76.png


### 代码示意

```go
func AlignScoreIndices(scores []int) []int {
	n := len(scores)
	if n <= 0 {
		return scores
	}
	// 深拷贝，防止对元数据做了修改
	sortedArr := make([]int, n)
	copy(sortedArr, scores)

	for i := 0; i < n-1; i++ {
		swapped := false
		for j := 0; j < n-1-i; j++ {
			if sortedArr[j] > sortedArr[j+1] {
				sortedArr[j], sortedArr[j+1] = sortedArr[j+1], sortedArr[j]
				swapped = true
			}
		}
		// 如果未发生交换，结束循环即可
		if !swapped {
			break
		}
	}
	return sortedArr
}
```

**为什么要进行两次循环？**

- 第二层循环是因为我们要的核心算法，将当前数据和下一个数对比，如果不符合顺序则交换，确保从头遍历到尾
- 第一次循环是因为，这样的遍历我们最多只需要 N次，n = 数列的长度。

**如何进行交换** 

- 在 Go 语法中交换不需要中间变量，直接用 = 一气呵成

**为什么要有 swapped 变量进行中止？** 

- 其实不需要 **swapped 也可以实现代码，两层循环全部完成即可**
- **增加 swapped 是因为，如果某次遍历，没有发生交换，说明顺序已经符合了，我们不用再进行无脑遍历，提升效率。**

### **性能评估**

/bc17744a-69c4-4672-b785-5acdf9f06884.png

通过性能测试发现：

1\. 不同数据规模测试（随机数组）  
•  BenchmarkBubbleSort10: 10个元素，8,311,653次运行，平均149.7纳秒，分配80B  
•  BenchmarkBubbleSort100: 100个元素，289,903次运行，平均4微秒，分配896B  
•  BenchmarkBubbleSort1000: 1000个元素，2,908次运行，平均422微秒，分配8KB  
•  BenchmarkBubbleSort5000: 5000个元素，123次运行，平均10毫秒，分配40KB  
<br>
2\. 场景对比测试（均为1000个元素）  
•  最好情况（已排序）: 1,457ns - 排序已完成，只遍历一次  
•  最坏情况（逆序）: 306,721ns - 需要最多次数交换  
•  平均情况（随机）: 414,602ns - 介于两者之间  
<br>
3\. 表驱动测试  
验证了不同规模下的性能递减趋势，从10到2000元素。


#### 关键发现

1. 时间复杂度验证：从 10->100 元素耗时增加了约 27倍，接近 O(n²)理论值
2. 内存分配: 每次都只分配1次（copy操作），大小=元素数×8字节
3. 最好vs最坏: 性能差异超过200倍，说明优化的冒泡排序有提前退出机制


### 个人感悟

首先，整体代码写下来还是有些磕巴，原因主要是将想法实现成代码的时候，细节 `i <=`  和 `i <` 的差异没有搞清楚导致频繁越界 panic 。

其次，真的和开头说的一样，写代码有些生疏，大量 AI 生成代码，虽然我会阅读和调试，但是从头写是不一样的。

综合看，这也符合我为什么做这件事的初衷，希望能够循序渐进，从易到难的推进这件事， 后续在刷一遍算法后逐步启动「感兴趣的项目和工具」开发，逐步找回编码的乐趣。